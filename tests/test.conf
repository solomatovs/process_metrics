# test.conf — правила для тестового прогона process_metrics
#
# Формат:  name = /regex_для_cmdline/
#
# Правило матчит корневой процесс по cmdline, далее отслеживаются
# все его потомки (fork-дерево).

# Тест 1: bash-дерево с CPU/memory нагрузкой через stress
test_tree = /test_process_tree/

# Тест 2: postgres (запуск через docker)
# postgres = /postgres/
